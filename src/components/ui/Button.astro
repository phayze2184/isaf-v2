---
import arrowRight from "../../assets/icons/arrowRight.svg";

const {
  href,
  variant = "primary",
  class: className,
  type = "button",
  ...attrs
} = Astro.props;

const Element = href ? "a" : "button";
const variants = ["primary", "secondary"];
const buttonVariant = variants.includes(variant) ? variant : "primary";
---

<Element
  {...attrs}
  href={href}
  type={href ? undefined : type}
  class={`button button--${buttonVariant} ${className ?? ""}`}
>
  <span class="button-label">
    <slot />
  </span>
  <span class="button-icon-area" aria-hidden="true">
    <span class="button-icon-decor"></span>
    <span
      class="button-icon"
      style={`--button-icon: url("${arrowRight.src}")`}
    ></span>
  </span>
</Element>

<style>
  .button {
    min-height: 3rem;
    box-sizing: border-box;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    border-radius: var(--radius-md);
    cursor: pointer;
    font-family: "Atkinson Hyperlegible", sans-serif;
    font-size: var(--font-size-button);
    font-weight: var(--font-weight-label);
    line-height: var(--line-height-130);
    letter-spacing: var(--letter-spacing-label);
    text-decoration: none;
    overflow: hidden;
    transition:
      background-color 400ms ease-in-out,
      color 400ms ease-in-out,
      transform 400ms ease-in-out;
  }

  .button-label {
    display: inline-flex;
    align-items: center;
    min-width: 0;
  }

  .button-icon-area {
    position: relative;
    z-index: 1;
    display: inline-grid;
    flex: none;
    place-items: center;
    transition:
      color 400ms ease-in,
      background-color 400ms ease-in-out;  
  }

  .button-icon-decor {
    display: none;
  }

  .button-icon {
    width: var(--size-icon-lg);
    height: var(--size-icon-lg);
    position: relative;
    z-index: 1;
    display: block;
    background: currentColor;
    -webkit-mask: var(--button-icon) center / contain no-repeat;
    mask: var(--button-icon) center / contain no-repeat;
    transition: transform 200ms ease;
  }

  .button:hover .button-icon,
  .button:focus-visible .button-icon {
    transform: rotate(-45deg);
  }

  .button:focus-visible {
    outline: var(--border-medium) solid var(--color-text-accent);
    outline-offset: var(--spacing-1);
  }

  .button:active {
    transform: translateY(1px);
  }

  .button[disabled],
  .button[aria-disabled="true"] {
    cursor: not-allowed;
    opacity: 0.6;
    pointer-events: none;
  }

  .button--primary {
    --button-primary-text: var(--color-button-primary-text);
    --button-primary-icon-bg: var(--color-button-primary-icon-bg);
    --button-primary-icon-area-size: 3rem;

    inline-size: fit-content;
    max-inline-size: 100%;
    block-size: 3rem;
    max-block-size: 3rem;
    justify-content: flex-start;
    gap: var(--spacing-6);
    padding: 0;
    border-radius: var(--radius-md);
    background: var(--color-button-primary-bg);
    color: var(--button-primary-text);
  }

  .button--primary:hover,
  .button--primary:focus-visible {
    --button-primary-text: var(--color-red-100);
    --button-primary-icon-bg: var(--color-button-primary-icon-bg);

    background: var(--color-red-700);
    color: var(--button-primary-text);
  }

  .button--primary .button-label {
    height: 100%;
    justify-content: center;
    padding: var(--spacing-3) 0 var(--spacing-3) var(--spacing-5);
    line-height: var(--line-height-130);
    white-space: nowrap;
  }

  .button--primary .button-icon-area {
    align-self: stretch;
    inline-size: var(--button-primary-icon-area-size);
    color: var(--button-primary-text);
    overflow: visible;
  }

  .button--primary .button-icon-decor {
    position: absolute;
    inset: 0 0 0 -39.58%;
    display: block;
    opacity: 1;
    background: var(--button-primary-icon-bg);
    clip-path: polygon(28.36% 0, 100% 0, 100% 100%, 0 100%);
    transition: background-color 200ms ease;
  }

  .button--primary .button-icon {
    width: var(--size-icon-lg);
    height: var(--size-icon-lg);
  }

  .button--primary:hover .button-icon-decor,
  .button--primary:focus-visible .button-icon-decor {
    background: var(--color-red-700);
  }

  .button--secondary {
    border: var(--border-hairline) solid var(--color-button-secondary-border);
    background: var(--color-button-secondary-bg);
    color: var(--color-button-secondary-text);
  }

  .button--secondary:hover {
    border-color: var(--color-amber);
    color: var(--color-amber);
  }

  @media (prefers-reduced-motion: reduce) {
    .button,
    .button-label,
    .button-icon,
    .button-icon-area,
    .button-icon-decor {
      transition-duration: 0.01ms;
    }
  }
</style>
